# Flux Media Creations Studio

Sanity Studio connected to project `2ljzfn1e` and the `production` dataset.

```bash
npm install
npm run dev
```

The website reads published `post` documents. Drafts remain private until published.
